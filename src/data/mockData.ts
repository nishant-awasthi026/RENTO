
import { User, Vehicle, Booking, Review } from "@/types";

// Empty mock data arrays - we'll use the real data from API
export const mockVehicles: Vehicle[] = [];
export const mockBookings: Booking[] = [];
export const mockReviews: Review[] = [];
